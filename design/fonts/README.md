# fonts

`monaspace-neon-latin-wght.woff2` is the house code face, vendored from
`oxagen-brand/fonts/` (SIL OFL, licence beside it). It is on no public CDN, so
`tools/lib/house-fonts.mjs` and `tools/build-docs.py` inline it into the pages
they build. Space Grotesk and Geist load from Google Fonts.
